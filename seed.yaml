# ---------------------------------------
# hf-proxy MapProxy seeding configuration.
# ---------------------------------------
#
# Defines cache seeding and cleanup schedules for hf-proxy.
# See full_seed_example.yaml and the documentation for more options.
#

seeds:

cleanups:

coverages:
